/**
 * اسکریپت اجرا‌کننده فرآیند واردسازی محتوا از TMDB
 * این فایل اسکریپت اصلی import-tmdb-content.ts را فراخوانی می‌کند
 */

// فقط واردکردن و اجرای فایل اصلی
import './import-tmdb-content';

// این فایل فقط به عنوان یک نقطه ورود ساده برای اجرای اسکریپت اصلی عمل می‌کند